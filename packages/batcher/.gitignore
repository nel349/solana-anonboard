# Keypairs — NEVER committed. The local batcher/sponsor key is generated on first run
# (scripts/dev.ts) and reused; each clone gets its own. Its pubkey is injected at runtime
# (VITE_BATCHER_FEE_PAYER / SOLANA_BATCHER_KEYPAIR), so no committed key is needed.
keypair/*.json
